<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for image picker and camera -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Permission for audio recording -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    
    <!-- Permissions for reminders and alarms -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteX">
        
        <!-- FileProvider for sharing files (PDF, images) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <!-- Splash Activity - Entry Point -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.NoteX.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Register Activity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
        
        <!-- User List Activity (Admin) -->
        <activity
            android:name=".UserListActivity"
            android:exported="false" />
        
        <!-- User Home Activity -->
        <activity
            android:name=".UserHomeActivity"
            android:exported="false" />
        
        <!-- Admin Dashboard Activity -->
        <activity
            android:name=".AdminDashboardActivity"
            android:exported="false" />
        
        <!-- Notebooks Activity -->
        <activity
            android:name=".NotebooksActivity"
            android:exported="false" />
        
        <!-- Create Notebook Activity -->
        <activity
            android:name=".CreateNotebookActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Notebook Pages Activity -->
        <activity
            android:name=".NotebookPagesActivity"
            android:exported="false" />
        
        <!-- Page Editor Activity -->
        <activity
            android:name=".PageEditorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Multi-Page Editor Activity -->
        <activity
            android:name=".MultiPageEditorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Scan Documents Activity (Document Library) -->
        <activity
            android:name=".ScanDocumentsActivity"
            android:exported="false" />
        
        <!-- Text Viewer Activity -->
        <activity
            android:name=".TextViewerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Scan Activity -->
        <activity
            android:name=".ScanActivity"
            android:exported="false" />
        
        <!-- Reminders Activity -->
        <activity
            android:name=".activities.RemindersActivity"
            android:exported="false" />
        
        <!-- Edit Reminder Activity -->
        <activity
            android:name=".activities.EditReminderActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Create Reminder Activity -->
        <activity
            android:name=".activities.CreateReminderActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Original MainActivity (kept for reference, not used) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        
        <!-- Broadcast Receivers for Reminders -->
        <receiver
            android:name=".ReminderReceiver"
            android:enabled="true"
            android:exported="false" />
        
        <receiver
            android:name=".ReminderActionReceiver"
            android:enabled="true"
            android:exported="false" />
        
        <receiver
            android:name=".GeofenceReceiver"
            android:enabled="true"
            android:exported="false" />
            
    </application>

</manifest>